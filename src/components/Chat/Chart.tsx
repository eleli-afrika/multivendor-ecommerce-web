import bg from '../../assets/chat.jpg';

const Chart = () => {
    return (
        <div
            className="h-[62.5vh] bg-red-200 w-full"
            style={{ backgroundImage: `url(${bg})` }}
        ></div>
    );
};

export default Chart;
