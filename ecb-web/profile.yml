info:
  name: profile
  type: http
  seq: 3

http:
  method: GET
  url: https://ecbe.eleliafrika.cloud/user/auth/profile
  headers:
    - name: Authorization
      value: "{{token}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
